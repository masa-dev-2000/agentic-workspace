# Verified failure cases

Empty. Fill this in from real prompt-quality incidents as they're found and
diagnosed (bad AI output reported by a user, an eval regression that took
longer than expected to root-cause, a `[feature:xxx]` tag mistake, etc.).
Follow the `implement-v2-work` skill's `references/failure-cases.md` format:
one `## F-00N: <short title>` section per case with Observed / Root cause /
Required prevention / Regression cases.
