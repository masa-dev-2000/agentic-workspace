interface:
  display_name: "Quality Protocol (Legacy)"
  short_description: "Explicit compatibility entry for legacy quality gates"
  default_prompt: "Use $quality-protocol to apply the legacy gated implementation protocol explicitly."
policy:
  allow_implicit_invocation: false
